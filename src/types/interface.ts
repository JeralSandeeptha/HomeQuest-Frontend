export interface NotFound {
    title?: string,
    header?: string,
    subHeader?: string,
    buttonText?: string,
    buttonUrl?: string,
}

export interface Blog {
    title: string,
    content: string,
    date: string,
}